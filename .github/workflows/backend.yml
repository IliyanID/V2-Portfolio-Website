name: Backend Build

on:
  push:
    paths:
      - 'backend/**'

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Build Backend
        run: ./infra/docker/frontend/build-docker.sh